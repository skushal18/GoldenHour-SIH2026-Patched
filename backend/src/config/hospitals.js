/* Bridge to the repo-level hackathon config, so the backend never carries a
   second copy of the two laptop IPs. Edit config/hospitals.config.js. */

'use strict';

module.exports = require('../../../config/hospitals.config');
